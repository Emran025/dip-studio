# ADR 0002: First Vertical Slice

The first feature slice is create/open a blank document, run an identity processor, render a preview frame, and expose it through a theme-aware Qt shell. It must prove domain, application, processing, rendering, persistence, and presentation boundaries before adding advanced algorithms.
